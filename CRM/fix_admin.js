require('dotenv').config({path: '.env.local'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); supabase.auth.admin.listUsers().then(({data, error}) => { if (error) console.error(error); else { const user = data.users.find(u => u.email === 'arpitmudgal24@gmail.com'); if (user) { console.log('Found user UID:', user.id); supabase.from('users').insert({id: user.id, email: user.email, name: 'Arpit Mudgal', role: 'ADMIN', active: true}).then(res => console.log('Inserted into public.users:', !res.error ? 'Success' : res.error)); } else console.log('User not found in auth.users'); } });
